# Core Lightning pour YunoHost

`core-lightning_ynh` empaquette un nœud Core Lightning géré par systemd et utilisant une installation locale de `bitcoin-core_ynh` comme backend Bitcoin.

Le paquet est encore un squelette de packaging. Avant toute utilisation avec des fonds, consultez `doc/BACKUP.md` et `doc/RECOVERY.md`.
