#:schema https://raw.githubusercontent.com/YunoHost/apps/main/schemas/tests.v1.schema.json

test_format = 1.0

[default]

    # The standard lifecycle tests are intentionally supplemented by regtest
    # integration tests before stable publication; see doc/TESTING.md.
