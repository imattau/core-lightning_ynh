# ARM64

ARM64 is a first-class target. The manifest consumes the signed upstream Ubuntu 22.04 arm64 archive, matching the amd64 packaging strategy. Test installation and the two-node regtest suite on real ARM64 YunoHost before catalog publication.
